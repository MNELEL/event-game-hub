## הוספת כפתור 'חזרה לדף הראשי' במסך /yemot-setup

### השינוי
בכותרת העליונה של `src/pages/YemotSetup.tsx` קיים כיום כפתור "חזרה לניהול" שמנווט אל `/admin`. נוסיף לידו כפתור נוסף "חזרה לדף הראשי" שמנווט אל `/` (Index).

### פירוט טכני
- קובץ יחיד שמשתנה: `src/pages/YemotSetup.tsx` (שורות ~191-193).
- נוסיף `<Button variant="ghost" size="sm" onClick={() => navigate("/")}>` עם אייקון `Home` מ-lucide-react ותווית "חזרה לדף הראשי".
- נוסיף `Home` לרשימת ה-imports מ-`lucide-react`.
- אין שינוי במבנה / לוגיקה / DB / edge functions.

### תוצאה
המשתמש רואה בראש הדף שני כפתורי ניווט: "חזרה לדף הראשי" ו-"חזרה לניהול", ויכול לעבור בלחיצה אחת בלי להשתמש בתפריט הניווט.